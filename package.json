{
  "name": "cucumber-js-test",
  "version": "0.0.0",
  "description": "> Some tests of using cucumber JS with zombie and selenium",
  "main": "index.js",
  "scripts": {
    "test-zombie": "./node_modules/.bin/cucumber.js features -r examples/zombie/support -r examples/zombie/steps",
    "test-phantom": "./node_modules/.bin/cucumber.js features -r examples/phantom/support -r examples/phantom/steps"
  },
  "devDependencies": {
    "cucumber": "^0.9.5",
    "phantom": "^2.0.4",
    "zombie": "^4.2.1"
  }
}
